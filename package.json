{
  "name": "roommate-tracker",
  "version": "1.0.0",
  "description": "Roommate Management Application",
  "main": "server/server.js",
  "scripts": {
    "install:all": "npm i && cd server && npm i && cd ../client && npm i",
    "start:dev": "concurrently -k \"cd server && npm run watch\" \"cd client && npm run electron-start\" \"wait-on http://localhost:3000 && cd client && electron .\""
  },
  "keywords": [],
  "author": "Zach Yarbrough",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.3.0",
    "wait-on": "^6.0.0"
  },
  "dependencies": {
    "electron": "^15.3.1",
    "jsonwebtoken": "^8.5.1"
  }
}
